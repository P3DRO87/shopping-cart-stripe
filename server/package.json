{
   "name": "server",
   "version": "1.0.0",
   "description": "",
   "main": "index.js",
   "engines": {
      "node": "14.x"
   },
   "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "build:client": "cd ../client && npm run-script build",
      "start:server": "NODE_ENV=production node index",
      "start": "npm run build:client && npm run start:server"
   },
   "keywords": [],
   "author": "",
   "license": "ISC",
   "dependencies": {
      "cors": "^2.8.5",
      "dotenv": "^16.0.2",
      "express": "^4.18.1",
      "stripe": "^10.6.0"
   }
}
